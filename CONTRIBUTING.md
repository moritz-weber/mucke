# Contributing to mucke

## Hints for building the app

`haudiotagger` requires JDK 17 to build. If this is not part of the standard path,
it might be required to tell Gradle or Flutter where to find it.

This can be done by running:

``` bash
flutter config --jdk-dir=<PATH TO JDK 17>
```